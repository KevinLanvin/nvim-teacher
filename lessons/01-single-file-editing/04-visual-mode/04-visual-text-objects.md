Cursor anywhere inside thisword, press viw — selects the whole word in one move, same "inner word" object from Campaign 3's ciw, just used to select first instead of change first. New combo unlocked. Press d to delete it and see the combo land, or press v again to cancel the selection without doing anything.

callSomething(select everything inside these parens)

Cursor anywhere inside the parens above, press vi( — instantly selects everything between them, no manual dragging needed. Same object you used for di( back in Campaign 3, just reordered: select then decide, instead of act then reach.

This is one paragraph chunk again, spanning a
few lines that belong together as a single
piece, exactly like the one from Campaign 3.

Cursor anywhere inside that chunk above, press vip — selects the whole chunk at once. From here you can press d, y, or c, whatever this level's combo calls for — the selection doesn't lock you into one action.

Ready for the next level? Cursor on the file below, gf to load it.

05-reselect-and-swap.md
