Welcome to Campaign 4: Visual mode. Same combo idea as before, just reordered — select first, decide the action second, instead of picking the action and reaching for a movement.

Select a few words on this line with v, then press d to delete the selection.

v starts a character-wise selection. New move unlocked: once it's active, any movement you already know extends it — try v then w a couple times on this line to grow the selection word by word, then d to delete whatever's highlighted.

    select from the middle of this to the end →

Cursor at the start of that line above, press v, then $ to extend all the way to the end, then y to yank the selection instead of deleting it. Movements you learned in Campaign 1 all work here exactly the same way.

Ready for the next level? Cursor on the file below, gf to load it.

02-line-visual.md
